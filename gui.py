from tkinter import *

master = Tk()

title = Label(master, text="Compare Game prices").grid(row=0)

l1 = Label(master, text="Search for Game:", justify=LEFT).grid(row=1)
e1 = Entry(master, )

e1.grid(row=1, column=1)

master.mainloop()